/*
function __resize__stack ( name, stack )
{
  var elem = document.getElementById( name );
  elem.style.width  = stack.canvas.width  + 'px';
  elem.style.height = stack.canvas.height + 'px';
  elem.width        = stack.canvas.width;
  elem.height       = stack.canvas.height;
}
*/
